#version 430

struct particle {
 vec3	currPos;
 vec3	speedVec;
 float ttl;
};

layout(location=0) in particle part;

uniform mat4 ViewMatrix;
uniform mat4 ProjectionMatrix;

void main(void)
{
  gl_Position = vec4(part.currPos, 1.0);
}
